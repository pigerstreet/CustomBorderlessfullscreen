pluginManagement {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
		gradlePluginPortal()
	}
}

plugins {
	// Lets Gradle download the Java 25 toolchain the game requires
	id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}
